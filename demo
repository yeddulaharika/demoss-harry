pipeline {
    agent any
    tools {
        maven "maven"
    }
    stages {
        stage("compile") {
            steps {
                sh "mvn compile"
            }
        }
        stage("install") {
            steps {
                sh "mvn install"
            }
        }
    }
}
